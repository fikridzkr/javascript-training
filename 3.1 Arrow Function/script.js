const tampilNama = (nama) => {
  return `halo ${nama}`;
};

// console.log(tampilNama("Erik"));

let siswa = ["messi", "mbappe", "Ronaldo"];

// let jumlahHuruf = siswa.map((nama) => nama.length);

console.log(jumlahHuruf);
